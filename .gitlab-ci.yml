image: docker:latest

services:
- docker:dind

before_script:
- docker info

build_images:
  stage: build
  script:
    - for im in labrobotica gitlabci django-ci openldap;do pushd ${im}; docker build -t ${im} .; popd; done
